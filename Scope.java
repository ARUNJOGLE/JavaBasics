package com.arun;

public class Scope {
    public static void main(String[] args) {
        /*
        Scope in Java
            -> Function Scope : cannot use in another functions
            -> Block Scope : Can't Initialise again(but can change the value) when already initialised outside the block. If initialised inside the block cannot be used outside the block
            -> Scoping in Loops
            -> Scope will begin when value is initialised!!
        */
    }
}
